%\documentclass[svgnames]{scrreprt}
%\documentclass[svgnames]{report}
%\usepackage{xcolor}
\usepackage[table,xcdraw]{xcolor}
\definecolor{primary}{RGB}{35,186,179}
\definecolor{secondary}{RGB}{82,155,182}
\usepackage{tikz} 
\usepackage{listings}
\usepackage{underscore}
\usepackage{graphicx}
\usepackage{float}
\usepackage[bookmarks=true]{hyperref}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{pdfpages}
\hypersetup{
    bookmarks=false,    % show bookmarks bar?
    pdftitle={Business Plan Report},    % title
    pdfauthor={Mohammad Hasan},                     % author
    colorlinks=true,       % false: boxed links; true: colored links
    linkcolor=secondary,       % color of internal links
    citecolor=secondary,       % color of links to bibliography
    filecolor=secondary,        % color of file links
    urlcolor=secondary,        % color of external links
    linktoc=page            % only page is linked
}%

\PassOptionsToPackage{hyphens}{url}
\usepackage{hyperref}
\usepackage{url}
\usepackage[explicit]{titlesec}
\newcommand*\chapterlabel{}
\titleformat{\chapter}
  {\gdef\chapterlabel{}
   \normalfont\sffamily\Huge\bfseries\scshape}
  {\gdef\chapterlabel{\thechapter\ }}{0pt}
  {\begin{tikzpicture}[remember picture,overlay]
    \node[yshift=-2cm] at (current page.north west)
      {\begin{tikzpicture}[remember picture, overlay]
      %fill=LightSkyBlue
        \draw[fill=primary] (0,0) rectangle
          (\paperwidth,3cm);
        \node[anchor=east,xshift=.9\paperwidth,rectangle,
              rounded corners=20pt,inner sep=11pt,
              %fill=MidnightBlue
              fill=secondary]
              {\color{white}\chapterlabel#1};
       \end{tikzpicture}
      };
   \end{tikzpicture}
  }
\titlespacing*{\chapter}{0pt}{50pt}{-60pt}

\usepackage{siunitx}

\sisetup{
  group-four-digits = true,
  group-separator = {,}
}

\usepackage{array}

\newenvironment{conditions}
  {\par\vspace{\abovedisplayskip}\noindent\begin{tabular}{>{$}l<{$} @{${}={}$} l}}
  {\end{tabular}\par\vspace{\belowdisplayskip}}
  

\usepackage{float}




